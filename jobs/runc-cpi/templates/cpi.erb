#!/bin/bash

if [[ `uname` == "Darwin" ]]; then
  os=darwin
else
  os=linux
fi

PACKAGES_DIR=${BOSH_PACKAGES_DIR:-/var/vcap/packages}
JOBS_DIR=${BOSH_JOBS_DIR:-/var/vcap/jobs}

${PACKAGES_DIR}/runc-cpi/bin/runc-cpi-${os} ${JOBS_DIR}/runc-cpi/config/config.yml