---
name: linuxkit-cpi

templates:
  cpi.erb: bin/cpi
  config.yml.erb: config/config.yml

packages:
- linuxkit-cpi

properties:
  linuxkit_cpi.network_type:
    description: "Connection mode (tcp|unix)"
    default: "unix"

  linuxkit_cpi.address:
    description: "Connection address"
    example: "(unix) /tmp/cpid.sock, (tcp) 127.0.0.1:9999"
    default: "/tmp/out-bosh-cpi-state/guest.00000946"
